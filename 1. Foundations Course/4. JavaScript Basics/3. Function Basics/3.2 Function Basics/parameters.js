// We can pass arbitrary data to functions using parameters.

function showMessage1(from, text) { // parameters: from, text
    alert(from + ': ' + text);
}

showMessage1('Ann', 'Hello!'); // Ann: Hello! (*)
showMessage1('Ann', "What's up?"); // Ann: What's up? (**)

















